services:
  latex-editor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: collaborative-latex-analytics
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
