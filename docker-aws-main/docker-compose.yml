services:
  collaborative-editor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: collaborative-code-analytics
    environment:
      NODE_ENV: production
      PORT: 3000
      EXECUTION_TIMEOUT_MS: 2500
    ports:
      - "3000:3000"
    restart: unless-stopped
